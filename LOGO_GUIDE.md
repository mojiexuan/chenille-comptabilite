# Logo 使用指南

## 📁 目录结构

请将 Logo 文件放置在以下位置：

```
E:\Project\Flutter\ChenilleComptabilite\
└── assets/
    └── images/
        ├── logo.png                # 启动页Logo（必需）
        ├── icon.png                # 应用图标主文件（必需）
        └── icon_foreground.png     # 自适应图标前景（可选）
```

## 🎨 文件要求

### 1. logo.png（启动页 Logo）

- **用途**: 显示在应用启动页
- **格式**: PNG
- **推荐尺寸**: 512×512px 或 1024×1024px
- **背景**: 透明背景最佳
- **圆角**: 代码会自动添加 30px 圆角
- **说明**: 如果文件不存在，会显示默认的宠物图标

### 2. icon.png（应用图标）

- **用途**: 手机桌面显示的应用图标
- **格式**: PNG
- **推荐尺寸**: 1024×1024px（高分辨率）
- **背景**: 不透明背景（或纯色背景）
- **说明**: 这是 Android 应用图标的主文件

### 3. icon_foreground.png（自适应图标前景，可选）

- **用途**: Android 8.0+ 自适应图标的前景层
- **格式**: PNG
- **推荐尺寸**: 1024×1024px
- **背景**: 透明背景
- **说明**:
  - 如果提供，会生成更现代的自适应图标
  - 前景图应该只包含主要图形，周围留白
  - 背景色在配置中设置为 `#FF8A65`（主题橙色）

## 🔧 生成应用图标

### 步骤 1: 准备文件

将 `icon.png`（和可选的 `icon_foreground.png`）放入 `assets/images/` 目录

### 步骤 2: 运行生成命令

```bash
# 安装依赖（如果还没有）
flutter pub get

# 生成应用图标
flutter pub run flutter_launcher_icons
```

### 步骤 3: 重新编译

```bash
flutter clean
flutter build apk
```

## 💡 设计建议

### 启动页 Logo (logo.png)

- ✅ 使用完整的品牌设计
- ✅ 可以包含细节和装饰元素
- ✅ 透明背景，让渐变背景自然透出
- ✅ 主体居中，四周留白适当

### 应用图标 (icon.png)

- ✅ 设计简洁明了，一眼可识别
- ✅ 在小尺寸下依然清晰可辨
- ✅ 使用品牌色（橙色系）
- ✅ 避免过多细节（在桌面图标很小）

### 自适应图标前景 (icon_foreground.png)

- ✅ 只包含核心图形（如毛虫形象）
- ✅ 周围留白至少 15%（安全区）
- ✅ 透明背景
- ✅ 前景图形在中心 66%区域内

## 🎯 示例规格

### Logo 色彩建议

- 主色: `#FF8A65` (橙色)
- 辅助色: `#FFB74D` (黄橙色)
- 点缀色: `#66BB6A` (绿色)

### 图标尺寸参考

```
启动页Logo:
- 原始: 1024×1024px
- 显示: 120×120dp

应用图标:
- 原始: 1024×1024px
- Android显示: 48-192dp（根据设备）
```

## ⚠️ 注意事项

1. **文件命名**: 必须严格按照上述命名，区分大小写
2. **文件格式**: 必须是 PNG 格式，不支持 JPG
3. **透明度**: logo.png 建议透明，icon.png 建议不透明
4. **版权**: 确保 Logo 设计不侵犯他人版权
5. **更新图标**: 生成新图标后，需要重新编译应用才能看到效果

## 🚀 快速检查清单

- [ ] 创建 `assets/images/` 目录
- [ ] 放置 `logo.png` (512×512 或 1024×1024)
- [ ] 放置 `icon.png` (1024×1024)
- [ ] （可选）放置 `icon_foreground.png` (1024×1024)
- [ ] 运行 `flutter pub get`
- [ ] 运行 `flutter pub run flutter_launcher_icons`
- [ ] 运行 `flutter clean`
- [ ] 重新编译应用测试效果

## 📞 遇到问题？

### Logo 不显示

- 检查文件路径是否正确
- 检查文件名是否完全匹配
- 运行 `flutter pub get` 重新加载资源
- 重启应用

### 应用图标没更新

- 确保运行了图标生成命令
- 运行 `flutter clean`
- 卸载旧应用，重新安装
- 检查手机启动器缓存

## 🎨 当前效果

未放置 Logo 时：

- 启动页显示默认宠物图标（橙色圆角方块）
- 应用图标使用 Flutter 默认图标

放置 Logo 后：

- 启动页显示您的自定义 Logo
- 应用图标显示您的品牌图标
